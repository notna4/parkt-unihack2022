import React, { useCallback, useMemo, useRef } from 'react';
import MapView, {Polyline, Marker} from 'react-native-maps';
import { StyleSheet, View, Text, Button  } from 'react-native';

function FreeSpots({short}) {
    return(
        <View>
            <Text>da</Text>
        </View>
    )
}